import { getIncidents } from './incident';
//import { app } from '../driver';


test('does jest work', () =>
{
	expect(2).toEqual(2);
});

describe('Incident endpoint tests', () =>
{
	test('get Incidents', async done =>
	{
		// const incidents = await getIncidents();
		// expect(incidents).toBe(true);
		// done();
	});
});
